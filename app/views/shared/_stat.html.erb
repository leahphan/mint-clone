<%# locals: (label:) -%>
<%# One labelled figure inside a .stat-strip; the block is the value. -%>
<div class="px-4 py-3">
  <p class="text-[11px] font-bold tracking-wide text-mint-muted uppercase"><%= label %></p>
  <p class="mt-0.5 text-xl"><%= yield %></p>
</div>
