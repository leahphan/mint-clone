<%# locals: (title: nil, css: nil) -%>
<section class="card <%= css %>">
  <% if title %>
    <div class="card-header">
      <h2 class="card-title"><%= title %></h2>
    </div>
  <% end %>
  <%= yield %>
</section>
