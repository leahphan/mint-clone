<% content_for :title, "Edit transaction" %>

<div class="mx-auto max-w-2xl">
  <%= render "shared/page_header", title: "Edit transaction", subtitle: @account.name %>

  <%= render layout: "shared/card" do %>
    <%= render "form", transaction: @transaction %>
  <% end %>
</div>
